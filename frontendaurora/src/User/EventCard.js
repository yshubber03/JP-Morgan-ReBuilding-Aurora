import React from 'react'

//Card component that will take in data from the backend and display it in the card
//data will include event description, event date, event time, event location, and event tools needed (if any)


const EventCard = () => {
    const eventDescription = "This is a test event description";
    const eventDate = "This is a test event date";
    const eventTime = "This is a test event time";
    const eventLocation = "This is a test event location";
    const eventTools = "These are the tools needed for this event";

  return (
    <div>

    </div>
  )
}

export default EventCard
